pipeline {
        stages {
            stage('Test') {
                steps {
                    sh 'robot -d logs/ web/tests'
                }
            }
            post {
                always {
                    robot outputPath: 'out/'
                    influxDbPublisher selectedTarget: 'myDb'
                }
            }
        }
}